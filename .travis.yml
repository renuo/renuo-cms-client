language: node_js
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - npm install -g bower tsd tslint karma gulp gulp-cli jshint
  - tsd reinstall --save --overwrite
  - npm install
cache:
  bundler: true
  directories:
    - node_modules
env:
  - MULTIPLE_BROWSERS="1"
